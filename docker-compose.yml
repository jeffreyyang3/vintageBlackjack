version: '3'
services:
  front:
    build: 
      context: ./front

    volumes:
      - /app/node_modules
      - ./front:/app
    
  nginx:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./nginx
    logging:
      driver: none
    ports:
      - '3050:80'

  api:
    build:
      dockerfile: Dockerfile
      context: ./back

    volumes:
      - /app/node_modules
      - ./back:/app
